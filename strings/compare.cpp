#include <iostream>
using namespace std;

int main() {
    string s1, s2;
    cin >> s1 >> s2;

    if(s1 == s2)
        cout << "Strings are Equal";
    else
        cout << "Strings are Not Equal";

    return 0;
}
